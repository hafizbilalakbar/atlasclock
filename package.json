{
  "name": "atlasclock",
  "version": "1.0.0",
  "description": "Global Time & World Clock Experience - Premium real-time time visualization platform",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch",
    "start": "npx serve . -l 3000",
    "preview": "npx serve . -l 3000",
    "clean": "if (Test-Path dist) { Remove-Item -Recurse -Force dist }"
  },
  "keywords": ["clock", "timezone", "world-clock", "time", "global"],
  "license": "MIT",
  "devDependencies": {
    "serve": "^14.2.0"
  }
}
